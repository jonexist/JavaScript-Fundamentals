//Array

const sampleArray = ['a', 'b', 'c', 'd'];

//methods to get the value
sampleArray[0];
sampleArray[1];
sampleArray[2];
sampleArray[3];

//sample methods in array

/* Add 1 value to the last line of the array */
sampleArray.push('e ');
/* Remove the last value inside the array */
sampleArray.pop();
/* Add value in the first part of the array */
sampleArray.unshift();